# Local LLM (Ollama) — matches host Ollama install, per docs/backend-standards.md
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3:8b

# Vector store (matches infra/docker-compose.yml's `chroma` service)
CHROMA_URL=http://chroma:8000
